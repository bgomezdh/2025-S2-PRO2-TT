/* bucles */

let paises = [ "Argentina", "USA" , "Francia", "Venezuela"];

for (let i = 0; i < paises.length; i++) {
    /* crear un constante para no llamar a cada rato  paises[i] */
    const el = paises[i];
    console.log(el);

}

